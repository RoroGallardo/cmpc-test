meta {
  name: Resolve Alert
  type: http
  seq: 16
}

patch {
  url: {{analytics_service_url}}/alerts/{{alert_id}}/resolve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Marca una alerta como resuelta.
  
  Cambia el estado a RESOLVED e indica que
  el problema ha sido solucionado.
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
