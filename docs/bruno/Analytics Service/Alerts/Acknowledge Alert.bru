meta {
  name: Acknowledge Alert
  type: http
  seq: 15
}

patch {
  url: {{analytics_service_url}}/alerts/{{alert_id}}/acknowledge
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Marca una alerta como reconocida.
  
  Cambia el estado de la alerta a ACKNOWLEDGED,
  indicando que un usuario ha visto la alerta.
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
